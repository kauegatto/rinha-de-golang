worker_rlimit_nofile 500000;

events {
	worker_connections 50000;
}

http {
	access_log off;

	upstream api {
		server api01:8080;
		server api02:8080;
	}

	server {
		listen 9999;

		location / {
			proxy_read_timeout 300s;
   			proxy_connect_timeout 75s;
			proxy_pass http://api;
		}
	}
}